/// @description emotes
// Você pode escrever seu código neste editor
if (alarm[10] == 0)
{
draw_clear_alpha(c_black,0);
}
else
{
alarm[10] = 0; // redefinir o alarme para zero para permitir o desenho da sprite novamente
}